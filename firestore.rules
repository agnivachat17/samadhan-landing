rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Samadhan uses server-side tRPC procedures with the Firebase Admin SDK.
    // Direct browser reads and writes are deliberately denied for the demo.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
