/** Style: Samadhan citizen challenge record — clear ownership sheet for report corrections and evidence. */
import PublicPortalHeader from "@/components/PublicPortalHeader";
import { FileUp, Loader2, Save } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { useLocation, useRoute } from "wouter";
import { trpc } from "@/lib/trpc";

export default function CitizenChallengeRecord() {
  const [, params] = useRoute("/citizen/challenges/:id"); const [, setLocation] = useLocation(); const id = Number(params?.id ?? 0); const query = useMemo(() => ({ id: id || 1 }), [id]); const evidenceInput = useMemo(() => ({ challengeId: id || 1 }), [id]); const challengeQuery = trpc.workflow.challengeById.useQuery(query, { enabled: id > 0 }); const evidenceQuery = trpc.workflow.challengeEvidence.useQuery(evidenceInput, { enabled: id > 0 }); const utils = trpc.useUtils(); const [title, setTitle] = useState(""); const [description, setDescription] = useState(""); const [domain, setDomain] = useState(""); const [district, setDistrict] = useState(""); const [file, setFile] = useState<File | null>(null); const update = trpc.workflow.updateChallenge.useMutation({ onSuccess: () => void utils.workflow.challengeById.invalidate() }); const upload = trpc.workflow.uploadChallengeEvidence.useMutation({ onSuccess: () => { void utils.workflow.challengeEvidence.invalidate(); setFile(null); } }); const challenge = challengeQuery.data;
  useEffect(() => { if (challenge) { setTitle(challenge.title); setDescription(challenge.description); setDomain(challenge.domain); setDistrict(challenge.district); } }, [challenge?.id]);
  async function uploadEvidence(event: React.FormEvent) { event.preventDefault(); if (!challenge || !file) return; if (file.size > 5_000_000) return; const base64 = await toBase64(file); upload.mutate({ challengeId: challenge.id, uploaderName: challenge.citizenName, fileName: file.name, mimeType: file.type || "application/octet-stream", base64 }); }
  return <main className="min-h-screen bg-[#f1eadc] text-[#0c3023]" style={{ backgroundImage: "url('/manus-storage/samadhan-paper-grain_46302c3f.jpg')", backgroundSize: "cover" }}><PublicPortalHeader /><section className="px-6 py-10 sm:px-10 lg:px-[5rem]"><div className="mx-auto max-w-[74rem]"><button type="button" onClick={() => setLocation("/citizen/dashboard")} className="font-body text-[0.78rem] text-[#496257] hover:text-[#c64b22]">← Back to My Submissions</button>{challengeQuery.isLoading ? <Loading /> : challengeQuery.isError || !challenge ? <Failure message={challengeQuery.error?.message || "Challenge record not found."} retry={() => void challengeQuery.refetch()} /> : <div className="mt-8 grid gap-9 xl:grid-cols-[minmax(0,1fr)_minmax(21rem,.64fr)]"><article><p className="font-mono-ui text-[0.62rem] font-semibold uppercase tracking-[0.13em] text-[#c64b22]">Citizen record · {challenge.status.replaceAll("_", " ")}</p><h1 className="mt-4 font-display text-[3.7rem] leading-[0.86] tracking-[-0.04em]">Keep this report accurate.</h1><p className="mt-4 font-body text-[0.88rem] leading-relaxed text-[#53675d]">This public-review page uses the report email as a demo identifier. Authentication and ownership enforcement can be enabled after review.</p><form onSubmit={event => { event.preventDefault(); update.mutate({ id: challenge.id, title, description, domain, district }); }} className="mt-7 border border-[#a58c6d]/55 bg-[#f8f2e8]/25 p-6"><label className="block"><span className="font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em]">Title</span><input value={title} onChange={event => setTitle(event.target.value)} className="citizen-input mt-2" required /></label><label className="mt-5 block"><span className="font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em]">Description</span><textarea value={description} onChange={event => setDescription(event.target.value)} className="citizen-input mt-2 min-h-[10rem] resize-y" required /></label><div className="mt-5 grid gap-5 sm:grid-cols-2"><label><span className="font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em]">Domain</span><input value={domain} onChange={event => setDomain(event.target.value)} className="citizen-input mt-2" required /></label><label><span className="font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em]">District</span><input value={district} onChange={event => setDistrict(event.target.value)} className="citizen-input mt-2" required /></label></div><button disabled={update.isPending} className="mt-6 inline-flex items-center gap-2 bg-[#16422f] px-5 py-3 font-mono-ui text-[0.57rem] font-semibold uppercase tracking-[0.1em] text-white"><Save size={15} />{update.isPending ? "Saving…" : "Save corrections"}</button>{update.isError && <p role="alert" className="mt-3 font-body text-[0.73rem] text-[#a34b2c]">{update.error.message}</p>}{update.isSuccess && <p className="mt-3 font-body text-[0.73rem] text-[#386548]">Your report changes have been saved.</p>}</form></article><aside className="space-y-6"><section className="border border-[#a58c6d]/55 bg-[#f8f2e8]/25 p-6"><p className="font-mono-ui text-[0.6rem] font-semibold uppercase tracking-[0.12em]">Evidence files</p>{evidenceQuery.isLoading ? <Loading /> : evidenceQuery.isError ? <Failure message={evidenceQuery.error.message} retry={() => void evidenceQuery.refetch()} /> : <div className="mt-4 space-y-3">{(evidenceQuery.data ?? []).map(item => <a key={item.id} href={item.fileUrl} target="_blank" rel="noreferrer" className="block border border-[#a58c6d]/45 p-3 font-body text-[0.77rem] hover:bg-[#e9dfcd]">{item.fileName}</a>)}{(evidenceQuery.data ?? []).length === 0 && <p className="font-body text-[0.75rem] text-[#61746a]">No evidence files have been uploaded.</p>}</div>}<form onSubmit={uploadEvidence} className="mt-5 border-t border-[#a78e6e]/45 pt-5"><input type="file" onChange={event => setFile(event.target.files?.[0] ?? null)} className="block w-full font-body text-[0.72rem]" /><button disabled={!file || upload.isPending} className="mt-4 flex w-full items-center justify-center gap-2 bg-[#c94a20] px-4 py-3 font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em] text-white disabled:opacity-50"><FileUp size={15} />{upload.isPending ? "Uploading…" : "Add evidence"}</button>{upload.isError && <p role="alert" className="mt-3 font-body text-[0.72rem] text-[#a34b2c]">{upload.error.message}</p>}</form></section><a href={`/challenges/${challenge.id}`} className="block border border-[#a58c6d]/55 px-5 py-4 font-body text-[0.78rem] font-semibold text-[#bd4a26]">View public workflow and support page →</a></aside></div>}</div></section></main>;
}
function Loading() { return <div className="mt-7 flex items-center gap-3 border border-[#a58c6d]/45 p-6 font-body text-[#52675d]"><Loader2 className="animate-spin" size={18} />Loading challenge record…</div>; }
function Failure({ message, retry }: { message: string; retry: () => void }) { return <div role="alert" className="mt-7 border border-[#bd5a38]/60 bg-[#f7e2d6]/35 p-6"><p className="font-body text-[0.78rem] text-[#934325]">{message}</p><button type="button" onClick={retry} className="mt-3 border border-[#bd5a38]/60 px-3 py-2 font-mono-ui text-[0.54rem] font-semibold uppercase tracking-[0.08em] text-[#a54426]">Retry</button></div>; }
function toBase64(file: File) { return new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onerror = () => reject(new Error("The selected file could not be read.")); reader.onload = () => resolve(String(reader.result)); reader.readAsDataURL(file); }); }
