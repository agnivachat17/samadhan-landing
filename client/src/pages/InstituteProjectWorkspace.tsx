/** Style: Samadhan institute project workspace — operational paper ledger for delivery, evidence, and activity. */
import InstituteHeader from "@/components/InstituteHeader";
import { FileUp, Flag, Loader2, Plus, Save, Send, Timer } from "lucide-react";
import { useEffect, useMemo, useState } from "react";
import { useLocation, useRoute } from "wouter";
import { trpc } from "@/lib/trpc";

export default function InstituteProjectWorkspace() {
  const [, params] = useRoute("/institute/projects/:id"); const [, setLocation] = useLocation(); const projectId = Number(params?.id ?? 0); const projectInput = useMemo(() => ({ id: projectId || 1 }), [projectId]); const recordsInput = useMemo(() => ({ projectId: projectId || 1 }), [projectId]);
  const projectQuery = trpc.workflow.projectById.useQuery(projectInput, { enabled: projectId > 0 }); const milestonesQuery = trpc.workflow.projectMilestones.useQuery(recordsInput, { enabled: projectId > 0 }); const documentsQuery = trpc.workflow.projectDocuments.useQuery(recordsInput, { enabled: projectId > 0 }); const activitiesQuery = trpc.workflow.projectActivities.useQuery(recordsInput, { enabled: projectId > 0 }); const utils = trpc.useUtils();
  const [progress, setProgress] = useState(0); const [stage, setStage] = useState("problem_identified"); const [status, setStatus] = useState("active"); const [riskSummary, setRiskSummary] = useState(""); const [note, setNote] = useState(""); const [milestoneTitle, setMilestoneTitle] = useState(""); const [milestoneDescription, setMilestoneDescription] = useState(""); const [documentFile, setDocumentFile] = useState<File | null>(null); const [documentType, setDocumentType] = useState("Working paper");
  const refresh = () => { void utils.workflow.projectById.invalidate(); void utils.workflow.projects.invalidate(); void utils.workflow.projectMilestones.invalidate(); void utils.workflow.projectDocuments.invalidate(); void utils.workflow.projectActivities.invalidate(); };
  const updateProject = trpc.workflow.updateProject.useMutation({ onSuccess: refresh }); const addMilestone = trpc.workflow.addMilestone.useMutation({ onSuccess: () => { refresh(); setMilestoneTitle(""); setMilestoneDescription(""); } }); const updateMilestone = trpc.workflow.updateMilestone.useMutation({ onSuccess: refresh }); const addActivity = trpc.workflow.addActivity.useMutation({ onSuccess: () => { refresh(); setNote(""); } }); const uploadDocument = trpc.workflow.uploadProjectDocument.useMutation({ onSuccess: () => { refresh(); setDocumentFile(null); } });
  const project = projectQuery.data;
  useEffect(() => {
    if (!project) return;
    setProgress(project.progress);
    setStage(project.stage);
    setStatus(project.status);
    setRiskSummary(project.riskSummary ?? "");
  }, [project?.id]);
  function saveProject() { if (!project) return; updateProject.mutate({ id: project.id, progress, stage: stage as any, status: status as any, riskSummary: riskSummary || undefined }); }
  function submitMilestone(event: React.FormEvent) { event.preventDefault(); if (!project) return; addMilestone.mutate({ projectId: project.id, title: milestoneTitle, description: milestoneDescription || undefined, position: (milestonesQuery.data?.length ?? 0) + 1 }); }
  async function submitDocument(event: React.FormEvent) { event.preventDefault(); if (!project || !documentFile) return; const base64 = await toBase64(documentFile); uploadDocument.mutate({ projectId: project.id, uploaderName: project.leadName, name: documentFile.name, documentType, mimeType: documentFile.type || "application/octet-stream", base64 }); }
  function submitNote(event: React.FormEvent) { event.preventDefault(); if (!project || !note.trim()) return; addActivity.mutate({ projectId: project.id, actorName: project.leadName, actorRole: "Institute lead", type: "note", title: "Workspace update", detail: note.trim() }); }
  return <main className="min-h-screen bg-[#f1eadc] text-[#0d3024]" style={{ backgroundImage: "url('/manus-storage/samadhan-paper-grain_46302c3f.jpg')", backgroundSize: "cover" }}><InstituteHeader active="Projects" /><section className="px-6 py-8 sm:px-10 lg:px-[4rem] lg:py-10"><div className="mx-auto max-w-[92rem]"><button type="button" onClick={() => setLocation("/institute/projects")} className="font-body text-[0.78rem] text-[#496257] hover:text-[#c64b22]">← Back to Active Projects</button>{projectQuery.isLoading ? <Loading /> : projectQuery.isError ? <Failure message={projectQuery.error.message} retry={() => void projectQuery.refetch()} /> : !project ? <Empty /> : <><div className="mt-8 flex flex-col justify-between gap-6 border-b border-[#a78e6e]/45 pb-8 lg:flex-row lg:items-end"><div><p className="font-mono-ui text-[0.62rem] font-semibold uppercase tracking-[0.13em] text-[#c64b22]">Project workspace · {project.status.replaceAll("_", " ")}</p><h1 className="mt-4 max-w-[56rem] font-display text-[3.5rem] leading-[0.86] tracking-[-0.04em] sm:text-[4.7rem]">{project.title}</h1><p className="mt-4 max-w-[52rem] font-body text-[0.88rem] leading-relaxed text-[#52675d]">{project.overview}</p></div><p className="font-mono-ui text-[0.57rem] font-semibold uppercase tracking-[0.1em] text-[#64776c]">Lead · {project.leadName}</p></div><div className="mt-8 grid gap-8 xl:grid-cols-[minmax(0,1.08fr)_minmax(22rem,.72fr)]"><div className="space-y-8"><section className="border border-[#a58c6d]/55 bg-[#f8f2e8]/25 p-6"><div className="flex items-center gap-3"><Flag className="text-[#c94a20]" size={20} /><p className="font-mono-ui text-[0.62rem] font-semibold uppercase tracking-[0.13em]">Delivery control</p></div><div className="mt-5 grid gap-5 sm:grid-cols-2"><WorkspaceSelect label="Stage" value={stage || project.stage} setValue={setStage} options={["problem_identified", "solution_design", "prototype_development", "pilot_testing", "closeout"]} /><WorkspaceSelect label="Status" value={status || project.status} setValue={setStatus} options={["active", "at_risk", "on_hold", "closeout_pending", "resolved"]} /><label><span className="font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em]">Progress · {progress || project.progress}%</span><input type="range" min="0" max="100" value={progress || project.progress} onChange={event => setProgress(Number(event.target.value))} className="mt-4 w-full accent-[#c94a20]" /></label><label><span className="font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em]">Risk or blocker</span><input defaultValue={project.riskSummary ?? ""} onChange={event => setRiskSummary(event.target.value)} className="citizen-input mt-3" placeholder="State blockers, if any" /></label></div><button type="button" disabled={updateProject.isPending} onClick={saveProject} className="mt-6 inline-flex items-center gap-2 bg-[#16422f] px-5 py-3 font-mono-ui text-[0.58rem] font-semibold uppercase tracking-[0.1em] text-white"><Save size={15} />{updateProject.isPending ? "Saving…" : "Save delivery update"}</button>{updateProject.isError && <p role="alert" className="mt-3 font-body text-[0.72rem] text-[#a34b2c]">{updateProject.error.message}</p>}</section><section><p className="border-b border-[#a78e6e]/45 pb-3 font-mono-ui text-[0.62rem] font-semibold uppercase tracking-[0.12em]">Milestones</p>{milestonesQuery.isLoading ? <Loading /> : milestonesQuery.isError ? <Failure message={milestonesQuery.error.message} retry={() => void milestonesQuery.refetch()} /> : <div className="mt-5 space-y-3">{(milestonesQuery.data ?? []).map(milestone => <div key={milestone.id} className="flex flex-col justify-between gap-3 border border-[#a58c6d]/45 p-4 sm:flex-row sm:items-center"><div><p className="font-display text-[1.35rem]">{milestone.title}</p><p className="mt-1 font-body text-[0.73rem] text-[#5c7066]">{milestone.description || "No description provided"}</p></div><select value={milestone.status} onChange={event => updateMilestone.mutate({ id: milestone.id, status: event.target.value as any })} className="border border-[#9a876c]/55 bg-transparent px-3 py-2 font-mono-ui text-[0.55rem] uppercase tracking-[0.08em]">{["upcoming", "in_progress", "complete", "blocked"].map(option => <option key={option} value={option}>{option.replaceAll("_", " ")}</option>)}</select></div>)}{(milestonesQuery.data ?? []).length === 0 && <Empty label="No milestones have been recorded." />}</div>}<form onSubmit={submitMilestone} className="mt-5 border border-dashed border-[#a58c6d]/55 p-5"><p className="font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em]">Add milestone</p><input required value={milestoneTitle} onChange={event => setMilestoneTitle(event.target.value)} className="citizen-input mt-3" placeholder="Milestone title" /><textarea value={milestoneDescription} onChange={event => setMilestoneDescription(event.target.value)} className="citizen-input mt-3 min-h-[5rem] resize-y" placeholder="Outcome, dependency, or definition of done" /><button disabled={addMilestone.isPending} className="mt-3 inline-flex items-center gap-2 bg-[#c94a20] px-4 py-3 font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em] text-white"><Plus size={14} />Add milestone</button></form></section><section><p className="border-b border-[#a78e6e]/45 pb-3 font-mono-ui text-[0.62rem] font-semibold uppercase tracking-[0.12em]">Activity record</p>{activitiesQuery.isError ? <Failure message={activitiesQuery.error.message} retry={() => void activitiesQuery.refetch()} /> : <div className="mt-5 space-y-3">{(activitiesQuery.data ?? []).map(activity => <div key={activity.id} className="border-l-2 border-[#8fa887] pl-4"><p className="font-body text-[0.79rem] font-semibold">{activity.title}</p><p className="mt-1 font-body text-[0.74rem] text-[#596e63]">{activity.detail || ""}</p></div>)}{(activitiesQuery.data ?? []).length === 0 && <Empty label="No delivery activity has been logged." />}</div>}<form onSubmit={submitNote} className="mt-5 flex gap-3"><input value={note} onChange={event => setNote(event.target.value)} className="citizen-input" placeholder="Log a material delivery update" /><button disabled={addActivity.isPending || !note.trim()} className="shrink-0 bg-[#16422f] px-4 text-white"><Send size={16} /></button></form></section></div><aside className="space-y-8"><section className="border border-[#a58c6d]/55 bg-[#f8f2e8]/25 p-6"><p className="flex items-center gap-2 font-mono-ui text-[0.62rem] font-semibold uppercase tracking-[0.12em]"><FileUp size={17} />Project documents</p>{documentsQuery.isLoading ? <Loading /> : documentsQuery.isError ? <Failure message={documentsQuery.error.message} retry={() => void documentsQuery.refetch()} /> : <div className="mt-5 space-y-3">{(documentsQuery.data ?? []).map(document => <a key={document.id} href={document.fileUrl} target="_blank" rel="noreferrer" className="block border border-[#a58c6d]/45 p-3 transition hover:bg-[#e7dfcf]"><p className="font-body text-[0.78rem] font-semibold">{document.name}</p><p className="mt-1 font-mono-ui text-[0.52rem] uppercase tracking-[0.08em] text-[#64776d]">{document.documentType} · {document.approvalStatus}</p></a>)}{(documentsQuery.data ?? []).length === 0 && <Empty label="No project documents uploaded." />}</div>}<form onSubmit={submitDocument} className="mt-5 border-t border-[#a78e6e]/45 pt-5"><label><span className="font-body text-[0.75rem]">Document type</span><input value={documentType} onChange={event => setDocumentType(event.target.value)} className="citizen-input mt-2" /></label><label className="mt-3 block"><span className="font-body text-[0.75rem]">File (up to 5 MB)</span><input type="file" onChange={event => setDocumentFile(event.target.files?.[0] ?? null)} className="mt-2 block w-full font-body text-[0.72rem]" /></label><button disabled={!documentFile || uploadDocument.isPending} className="mt-4 flex w-full items-center justify-center gap-2 bg-[#c94a20] px-4 py-3 font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em] text-white disabled:opacity-50"><FileUp size={15} />{uploadDocument.isPending ? "Uploading…" : "Upload document"}</button>{uploadDocument.isError && <p role="alert" className="mt-3 font-body text-[0.72rem] text-[#a34b2c]">{uploadDocument.error.message}</p>}</form></section><section className="border border-[#a58c6d]/55 bg-[#f8f2e8]/25 p-6"><p className="flex items-center gap-2 font-mono-ui text-[0.62rem] font-semibold uppercase tracking-[0.12em]"><Timer size={17} />Team record</p><p className="mt-4 whitespace-pre-wrap font-body text-[0.78rem] leading-relaxed text-[#51685d]">{project.teamMembers || "Team members have not yet been recorded."}</p></section></aside></div></>}</div></section></main>;
}
function WorkspaceSelect({ label, value, setValue, options }: { label: string; value: string; setValue: (value: string) => void; options: string[] }) { return <label><span className="font-mono-ui text-[0.56rem] font-semibold uppercase tracking-[0.1em]">{label}</span><select value={value} onChange={event => setValue(event.target.value)} className="citizen-input mt-3 capitalize">{options.map(option => <option key={option} value={option}>{option.replaceAll("_", " ")}</option>)}</select></label>; }
function Loading() { return <div className="mt-5 flex items-center gap-3 border border-[#a58c6d]/45 p-5 font-body text-[0.76rem] text-[#52675d]"><Loader2 className="animate-spin" size={17} />Loading project workspace…</div>; }
function Empty({ label = "Project record not found." }: { label?: string }) { return <div className="mt-5 border border-dashed border-[#a58c6d]/55 p-5 font-body text-[0.76rem] text-[#586d63]">{label}</div>; }
function Failure({ message, retry }: { message: string; retry: () => void }) { return <div role="alert" className="mt-5 border border-[#bd5a38]/60 bg-[#f7e2d6]/35 p-5"><p className="font-body text-[0.75rem] text-[#934325]">{message}</p><button type="button" onClick={retry} className="mt-3 border border-[#bd5a38]/60 px-3 py-2 font-mono-ui text-[0.53rem] font-semibold uppercase tracking-[0.08em] text-[#a54426]">Retry</button></div>; }
function toBase64(file: File) { return new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onerror = () => reject(new Error("The selected file could not be read.")); reader.onload = () => resolve(String(reader.result)); reader.readAsDataURL(file); }); }
