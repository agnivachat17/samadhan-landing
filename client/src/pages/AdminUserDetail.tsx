import AdminHeader from "@/components/AdminHeader";
import { Building2, Loader2, UserRound } from "lucide-react";
import { useMemo, useState } from "react";
import { useRoute } from "wouter";
import { trpc } from "@/lib/trpc";

export default function AdminUserDetail() {
  const [, params] = useRoute("/admin/users/:email"); const email = decodeURIComponent(params?.email ?? ""); const [input] = useState({}); const organizationsQuery = trpc.workflow.organizations.useQuery(input); const challengesQuery = trpc.workflow.challenges.useQuery(input); const notificationsQuery = trpc.workflow.notifications.useQuery({ recipientEmail: email || "placeholder@example.invalid" }, { enabled: Boolean(email) }); const organization = useMemo(() => (organizationsQuery.data ?? []).find(item => item.contactEmail.toLowerCase() === email.toLowerCase()), [organizationsQuery.data, email]); const citizenChallenges = (challengesQuery.data ?? []).filter(item => item.citizenEmail?.toLowerCase() === email.toLowerCase()); const loading = organizationsQuery.isLoading || challengesQuery.isLoading || notificationsQuery.isLoading; const error = organizationsQuery.error || challengesQuery.error || notificationsQuery.error;
  return <main className="min-h-screen bg-[#f1eadc] text-[#0d3024]" style={{ backgroundImage: "url('/manus-storage/samadhan-paper-grain_46302c3f.jpg')", backgroundSize: "cover" }}><AdminHeader active="Users" /><section className="px-6 py-10 sm:px-10 lg:px-[5rem]"><div className="mx-auto max-w-[76rem]"><a href="/admin/users" className="font-body text-[0.78rem] text-[#496257] hover:text-[#c64b22]">← Back to User Accounts</a>{loading ? <Loading /> : error ? <Failure message={error.message} retry={() => { void organizationsQuery.refetch(); void challengesQuery.refetch(); void notificationsQuery.refetch(); }} /> : <div className="mt-8 grid gap-8 xl:grid-cols-[minmax(0,1fr)_minmax(20rem,.55fr)]"><article><p className="font-mono-ui text-[0.6rem] font-semibold uppercase tracking-[0.12em] text-[#c64b22]">Detailed user workspace</p><h1 className="mt-4 break-all font-display text-[3.6rem] leading-[0.86] tracking-[-0.04em]">{organization?.name || citizenChallenges[0]?.citizenName || "Citizen contact"}</h1><p className="mt-4 font-body text-[0.9rem] text-[#53675d]">{email}</p><section className="mt-8 border border-[#a58c6d]/55 bg-[#f8f2e8]/25 p-6"><p className="font-mono-ui text-[0.58rem] uppercase tracking-[0.1em]">Record classification</p><div className="mt-5 flex items-center gap-3">{organization ? <Building2 size={24} className="text-[#315947]" /> : <UserRound size={24} className="text-[#315947]" />}<p className="font-display text-[1.8rem]">{organization ? `${organization.kind} · ${organization.verificationStatus}` : "Citizen reporter"}</p></div>{organization && <dl className="mt-6 grid gap-4 sm:grid-cols-2"><Pair label="Website" value={organization.website || "Not provided"} /><Pair label="Location" value={organization.location || "Not provided"} /><Pair label="Support / expertise" value={organization.supportModes || organization.expertise || "Not provided"} /><Pair label="Contact number" value={organization.contactPhone || "Not provided"} /></dl>}</section><section className="mt-8"><p className="border-b border-[#a78e6e]/45 pb-3 font-mono-ui text-[0.6rem] uppercase tracking-[0.1em]">Citizen challenge records</p>{citizenChallenges.length === 0 ? <p className="mt-5 font-body text-[0.78rem] text-[#607168]">No citizen-reported challenge is associated with this email.</p> : <div className="mt-5 space-y-3">{citizenChallenges.map(item => <a key={item.id} href={`/admin/challenges/${item.id}`} className="block border border-[#a58c6d]/45 p-4 hover:bg-[#e9dfcd]/50"><p className="font-display text-[1.4rem] leading-none">{item.title}</p><p className="mt-2 font-body text-[0.72rem] text-[#5c7066]">{item.status.replaceAll("_", " ")} · {item.district}</p></a>)}</div>}</section></article><aside className="border border-[#a58c6d]/55 bg-[#f8f2e8]/25 p-6"><p className="font-mono-ui text-[0.6rem] uppercase tracking-[0.1em]">Notification history</p>{(notificationsQuery.data ?? []).length === 0 ? <p className="mt-5 font-body text-[0.78rem] text-[#607168]">No workflow notices recorded.</p> : <div className="mt-5 space-y-3">{notificationsQuery.data?.map(item => <a key={item.id} href={item.href || "#top"} className="block border border-[#a58c6d]/45 p-4"><p className="font-body text-[0.78rem] font-semibold">{item.title}</p><p className="mt-2 font-body text-[0.72rem] text-[#5c7066]">{item.body}</p></a>)}</div>}</aside></div>}</div></section></main>;
}
function Pair({ label, value }: { label: string; value: string }) { return <div><dt className="font-mono-ui text-[0.52rem] uppercase tracking-[0.08em] text-[#64776d]">{label}</dt><dd className="mt-2 font-body text-[0.76rem] leading-relaxed text-[#53675d]">{value}</dd></div>; }
function Loading() { return <div className="mt-8 flex items-center gap-3 font-body text-[#52675d]"><Loader2 className="animate-spin" size={18} />Loading user workspace…</div>; }
function Failure({ message, retry }: { message: string; retry: () => void }) { return <div role="alert" className="mt-8 border border-[#bd5a38]/60 bg-[#f7e2d6]/35 p-6"><p className="font-body text-[0.76rem] text-[#934325]">{message}</p><button type="button" onClick={retry} className="mt-3 border border-[#bd5a38]/60 px-3 py-2 font-mono-ui text-[0.54rem] uppercase tracking-[0.08em] text-[#a54426]">Retry</button></div>; }
