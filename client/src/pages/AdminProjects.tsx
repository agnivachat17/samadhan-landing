/** Style: Samadhan project governance ledger with Firestore-backed delivery records. */
import AdminHeader from "@/components/AdminHeader";
import { ChevronRight, Loader2, Search } from "lucide-react";
import { useMemo, useState } from "react";
import { trpc } from "@/lib/trpc";

export default function AdminProjects() {
  const [query, setQuery] = useState(""); const [status, setStatus] = useState("all"); const [input] = useState({}); const projectsQuery = trpc.workflow.projects.useQuery(input); const organizationsQuery = trpc.workflow.organizations.useQuery(input); const closeoutsQuery = trpc.workflow.projectCloseouts.useQuery(input); const organizations = organizationsQuery.data ?? []; const visible = useMemo(() => (projectsQuery.data ?? []).filter(project => (status === "all" || project.status === status) && `${project.title} ${organizations.find(org => org.id === project.organizationId)?.name ?? ""}`.toLowerCase().includes(query.toLowerCase())), [projectsQuery.data, organizations, status, query]);
  return <main className="min-h-screen bg-[#f1eadc] text-[#0d3024]" style={{ backgroundImage: "url('/manus-storage/samadhan-paper-grain_46302c3f.jpg')", backgroundSize: "cover" }}><AdminHeader active="Projects" /><section className="px-6 py-10 sm:px-10 lg:px-[2.8rem] lg:py-10"><div className="mx-auto max-w-[96rem]"><div className="border-b border-[#a78e6e]/45 pb-7"><h1 className="font-display text-[3.8rem] font-medium leading-[0.86] tracking-[-0.04em] sm:text-[5rem]">Project Management</h1><p className="mt-4 font-body text-[0.9rem] text-[#53675d]">Monitor institute delivery, closeout readiness, evidence, and risks across persisted project records.</p></div><div className="mt-6 grid gap-4 sm:grid-cols-[1.5fr_.7fr]"><label className="flex h-[3.48rem] items-center gap-3 border border-[#a58c6d]/55 bg-[#f8f2e8]/28 px-4"><Search size={21} /><input value={query} onChange={event => setQuery(event.target.value)} placeholder="Search project or institution…" className="min-w-0 flex-1 bg-transparent font-body text-[0.8rem] outline-none" /></label><label className="border border-[#a58c6d]/55 bg-[#f8f2e8]/28 px-4 py-2"><span className="block font-mono-ui text-[0.55rem] font-semibold uppercase tracking-[0.12em]">Lifecycle status</span><select value={status} onChange={event => setStatus(event.target.value)} className="mt-1 w-full bg-transparent font-body text-[0.78rem] outline-none"><option value="all">All statuses</option>{["active", "at_risk", "on_hold", "closeout_pending", "resolved"].map(option => <option key={option} value={option}>{option.replaceAll("_", " ")}</option>)}</select></label></div><div className="mt-6 hidden grid-cols-[minmax(20rem,1.45fr)_.95fr_.75fr_.95fr_.65fr_7rem] gap-4 border-b border-[#a78e6e]/40 pb-4 font-mono-ui text-[0.59rem] font-semibold uppercase tracking-[0.1em] text-[#314a40] lg:grid"><span>Project</span><span>Institution</span><span>Stage</span><span>Progress</span><span>Status</span><span>Action</span></div>{projectsQuery.isLoading || organizationsQuery.isLoading || closeoutsQuery.isLoading ? <Loading /> : projectsQuery.isError || organizationsQuery.isError || closeoutsQuery.isError ? <Failure message={projectsQuery.error?.message || organizationsQuery.error?.message || closeoutsQuery.error?.message || "Project records could not load."} retry={() => { void projectsQuery.refetch(); void organizationsQuery.refetch(); void closeoutsQuery.refetch(); }} /> : <div>{visible.map(project => { const institution = organizations.find(org => org.id === project.organizationId); const closeout = (closeoutsQuery.data ?? []).find(item => item.projectId === project.id); return <article key={project.id} className="grid gap-4 border-b border-[#a78e6e]/40 py-5 lg:grid-cols-[minmax(20rem,1.45fr)_.95fr_.75fr_.95fr_.65fr_7rem] lg:items-center lg:gap-4 lg:px-3"><div><a href={`/admin/projects/${project.id}`} className="group"><h2 className="font-display text-[1.28rem] leading-none group-hover:text-[#b84a28] sm:text-[1.45rem]">{project.title}</h2></a>{project.riskSummary && <p className="mt-2 font-body text-[0.7rem] text-[#b54a28]">Risk: {project.riskSummary}</p>}</div><p className="font-body text-[0.74rem] leading-snug text-[#354e43]">{institution?.name ?? "Institution record"}</p><p className="font-body text-[0.75rem]">{project.stage.replaceAll("_", " ")}</p><div className="flex items-center gap-3"><span className="w-8 font-body text-[0.76rem] font-semibold">{project.progress}%</span><span className="h-1 flex-1 bg-[#d8cfbd]"><span className="block h-full bg-[#c64b22]" style={{ width: `${project.progress}%` }} /></span></div><span className="w-fit border border-[#839c82] px-2 py-1.5 font-mono-ui text-[0.54rem] font-semibold uppercase tracking-[0.08em] text-[#426548]">{closeout ? `closeout ${closeout.adminStatus}` : project.status.replaceAll("_", " ")}</span><div className="flex flex-col gap-2"><a href={`/admin/projects/${project.id}`} className="inline-flex items-center gap-1 font-body text-[0.75rem] font-semibold text-[#b94b27]">Details<ChevronRight size={16} /></a>{closeout && <a href={`/admin/projects/${project.id}/closeout`} className="font-body text-[0.72rem] font-semibold text-[#315947]">Review closeout</a>}</div></article>; })}{visible.length === 0 && <Empty />}</div>}</div></section></main>;
}
function Loading() { return <div className="mt-7 flex items-center gap-3 p-6 font-body text-[#52675d]"><Loader2 className="animate-spin" size={18} />Loading project governance records…</div>; }
function Empty() { return <div className="mt-7 border border-dashed border-[#a58c6d]/55 p-8 text-center font-body text-[0.8rem] text-[#586d63]">No matching persisted projects.</div>; }
function Failure({ message, retry }: { message: string; retry: () => void }) { return <div role="alert" className="mt-7 border border-[#bd5a38]/60 bg-[#f7e2d6]/35 p-6"><p className="font-body text-[0.76rem] text-[#934325]">{message}</p><button type="button" onClick={retry} className="mt-3 border border-[#bd5a38]/60 px-3 py-2 font-mono-ui text-[0.54rem] font-semibold uppercase tracking-[0.08em] text-[#a54426]">Retry</button></div>; }
