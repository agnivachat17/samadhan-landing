/** Style: Samadhan governance settings — operational readout of public-review and workflow controls. */
import AdminHeader from "@/components/AdminHeader";
import { Database, Loader2, ShieldCheck } from "lucide-react";
import { useMemo, useState } from "react";
import { trpc } from "@/lib/trpc";

export default function AdminSettings() {
  const [input] = useState({}); const organizationsQuery = trpc.workflow.organizations.useQuery(input); const challengesQuery = trpc.workflow.challenges.useQuery(input); const projectsQuery = trpc.workflow.projects.useQuery(input); const closeoutsQuery = trpc.workflow.projectCloseouts.useQuery(input); const loading = organizationsQuery.isLoading || challengesQuery.isLoading || projectsQuery.isLoading || closeoutsQuery.isLoading; const error = organizationsQuery.error || challengesQuery.error || projectsQuery.error || closeoutsQuery.error;
  const audit = useMemo(() => [ ...(organizationsQuery.data ?? []).map(item => ({ id: `org-${item.id}`, date: item.updatedAt ?? item.createdAt, kind: "Organization", title: item.name, detail: `Verification: ${item.verificationStatus}` })), ...(challengesQuery.data ?? []).map(item => ({ id: `challenge-${item.id}`, date: item.updatedAt ?? item.createdAt, kind: "Challenge", title: item.title, detail: `Status: ${item.status}` })), ...(projectsQuery.data ?? []).map(item => ({ id: `project-${item.id}`, date: item.updatedAt ?? item.createdAt, kind: "Project", title: item.title, detail: `Status: ${item.status}` })), ...(closeoutsQuery.data ?? []).map(item => ({ id: `closeout-${item.id}`, date: item.updatedAt ?? item.createdAt, kind: "Closeout", title: `Project ${item.projectId}`, detail: `Citizen: ${item.citizenConfirmation}; admin: ${item.adminStatus}` })) ].sort((a, b) => new Date(b.date ?? 0).getTime() - new Date(a.date ?? 0).getTime()).slice(0, 18), [organizationsQuery.data, challengesQuery.data, projectsQuery.data, closeoutsQuery.data]);
  return <main className="min-h-screen bg-[#f1eadc] text-[#0d3024]" style={{ backgroundImage: "url('/manus-storage/samadhan-paper-grain_46302c3f.jpg')", backgroundSize: "cover" }}><AdminHeader active="Settings" /><section className="px-6 py-10 sm:px-10 lg:px-[4rem]"><div className="mx-auto max-w-[90rem]"><p className="font-mono-ui text-[0.62rem] font-semibold uppercase tracking-[0.13em] text-[#c64b22]">Governance settings & audit</p><h1 className="mt-4 font-display text-[4.2rem] leading-[0.86] tracking-[-0.04em]">Workflow control room.</h1><p className="mt-5 max-w-[54rem] font-body text-[0.88rem] leading-relaxed text-[#53675d]">This public-review administration surface exposes the persisted workflow state, Firestore access boundary, and audit events. Role-based access control remains deliberately deferred until design review is complete.</p>{loading ? <Loading /> : error ? <Failure message={error.message} retry={() => { void organizationsQuery.refetch(); void challengesQuery.refetch(); void projectsQuery.refetch(); void closeoutsQuery.refetch(); }} /> : <><div className="mt-8 grid gap-5 sm:grid-cols-2 xl:grid-cols-4"><Card label="Pending institution reviews" value={(organizationsQuery.data ?? []).filter(item => item.kind === "institution" && item.verificationStatus === "pending").length} /><Card label="Challenges in review" value={(challengesQuery.data ?? []).filter(item => item.status === "under_review").length} /><Card label="Projects at risk" value={(projectsQuery.data ?? []).filter(item => item.status === "at_risk").length} /><Card label="Closeouts awaiting decision" value={(closeoutsQuery.data ?? []).filter(item => item.adminStatus === "pending").length} /></div><div className="mt-8 grid gap-7 xl:grid-cols-[minmax(0,1.15fr)_minmax(20rem,.55fr)]"><section><p className="border-b border-[#a78e6e]/45 pb-3 font-mono-ui text-[0.62rem] font-semibold uppercase tracking-[0.12em]">Recent audit events</p><div className="mt-5 space-y-3">{audit.map(event => <article key={event.id} className="border-l-2 border-[#8fa887] bg-[#f8f2e8]/20 p-4"><p className="font-mono-ui text-[0.52rem] uppercase tracking-[0.08em] text-[#64776d]">{event.kind} · {event.date ? new Date(event.date).toLocaleString() : ""}</p><h2 className="mt-2 font-display text-[1.4rem] leading-none">{event.title}</h2><p className="mt-2 font-body text-[0.74rem] text-[#53675d]">{event.detail}</p></article>)}{audit.length === 0 && <p className="font-body text-[0.78rem] text-[#607168]">No persisted audit events are available yet.</p>}</div></section><aside className="space-y-5"><section className="border border-[#a58c6d]/55 bg-[#f8f2e8]/25 p-6"><ShieldCheck className="text-[#315947]" size={26} /><p className="mt-4 font-mono-ui text-[0.58rem] font-semibold uppercase tracking-[0.1em]">Data-access boundary</p><p className="mt-3 font-body text-[0.78rem] leading-relaxed text-[#53675d]">Firestore rules deny direct browser reads and writes. The Samadhan server is the controlled tRPC write boundary, using the server-only Firebase Admin SDK.</p></section><section className="border border-[#a58c6d]/55 bg-[#f8f2e8]/25 p-6"><Database className="text-[#c94a20]" size={26} /><p className="mt-4 font-mono-ui text-[0.58rem] font-semibold uppercase tracking-[0.1em]">Hackathon tier scope</p><p className="mt-3 font-body text-[0.78rem] leading-relaxed text-[#53675d]">The implementation uses Firestore under the Spark-tier allowance and browser analytics only. Cloud Functions, Cloud SQL, Firebase SQL Connect, and Firebase Authentication are not required for this review build.</p></section></aside></div></>}</div></section></main>;
}
function Card({ label, value }: { label: string; value: number }) { return <article className="border border-[#a58c6d]/55 px-6 py-7"><p className="font-display text-[4rem] leading-none text-[#153e2d]">{String(value).padStart(2, "0")}</p><p className="mt-5 font-mono-ui text-[0.58rem] font-semibold uppercase tracking-[0.1em] text-[#53675d]">{label}</p></article>; }
function Loading() { return <div className="mt-8 flex items-center gap-3 font-body text-[#52675d]"><Loader2 className="animate-spin" size={18} />Loading governance records…</div>; }
function Failure({ message, retry }: { message: string; retry: () => void }) { return <div role="alert" className="mt-8 border border-[#bd5a38]/60 bg-[#f7e2d6]/35 p-6"><p className="font-body text-[0.76rem] text-[#934325]">{message}</p><button type="button" onClick={retry} className="mt-3 border border-[#bd5a38]/60 px-3 py-2 font-mono-ui text-[0.54rem] uppercase tracking-[0.08em] text-[#a54426]">Retry</button></div>; }
